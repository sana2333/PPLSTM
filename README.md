## Download Checkpoints

Model weights are hosted in the **Releases** section. Please download and extract the files to the project's root directory. 

Note that models with the `_s` suffix have an LSTM hidden dimension of **64**, while standard models (without the suffix) have a hidden dimension of **128**.
